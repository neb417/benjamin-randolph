<%= turbo_frame_tag "notices"%>
  <% flash.each do |flash_type, message| %>
    <div class="flash-container <%= flash_type %>" data-controller="delay-remove">
      <div class="flash-message">
        <%= message %>
      </div>
    </div>
  <% end %>
